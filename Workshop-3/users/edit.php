<?php
    // Retrieve the URL variables (using PHP).
    $userID = $_GET['id'];
    echo "The user with the id $userID is going to be edited";
?>
<!-- Get the id, search the info in the database, if it appears in the database, open the signup page with the information ready -->
